= simple_form_for(@room, html: {class: 'form-horizontal' }) do |f|
  = f.error_notification

  .form-inputs
    = f.input :name
    = f.input :description
    = f.association :world

  .form-actions
    = f.button :submit
